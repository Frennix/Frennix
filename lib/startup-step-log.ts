/** @deprecated Import from `@/lib/startup-snapshot-log` — kept for existing imports. */
export {
  logStartupStep,
  recordStartupSnapshot,
  getStartupSnapshots,
  getStartupCapturedErrors,
  buildStartupAuthState,
  parseWebEnvironment,
  collectDomStartupState,
  installStartupSnapshotCapture,
  type StartupSnapshotPhase as StartupStep,
} from "@/lib/startup-snapshot-log";
