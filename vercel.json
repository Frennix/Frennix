{
  "installCommand": "echo \"Using prebuilt dist from repo — workspace packages are built locally before push\"",
  "buildCommand": "test -f dist/index.html || (echo \"dist/index.html missing — run pnpm run build:web locally and commit dist\" && exit 1)",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/((?!_expo/).*)", "destination": "/index.html" }]
}
