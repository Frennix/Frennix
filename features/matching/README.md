# Matching module (deferred)

Swipe-based partner discovery will live here. Database tables `matches` and `match_swipes` are ready.

Profile fields used for matching:
- `matching_enabled`
- `gender`
- `match_preference` (`same` | `opposite` | `any`)

Enable via feature flag when launching matching.
